#  サブドメインのオーナー、グループをsftpユーザーに変更
- name: chown <subdomain>.xxx.xxx directory
  file: >-
    dest={{ www_dir }}/{{ subdomain }}.{{ domain }}
    state=directory
    recurse=yes
    owner={{ sftpuser }}
    group={{ sftpgroup }}
